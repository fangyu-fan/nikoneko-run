name: nikoneko-run
options:
  bundleIdPrefix: com.fangyu
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"
  createIntermediateGroups: true
  groupSortPosition: top
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: 6.0
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
    DEVELOPMENT_TEAM: B58R97M2WC

packages:
  Lottie:
    url: https://github.com/airbnb/lottie-ios
    minorVersion: "4.5.0"

targets:
  nikoneko:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    bundleId: com.fangyu.nikoneko
    sources:
      - path: nikoneko_run/App
      - path: nikoneko_run/Core
      - path: nikoneko_run/Features
      - path: nikoneko_run/Services
      - path: nikoneko_run/Characters
      - path: nikoneko_run/LiveActivity
      - path: nikoneko_run/Resources
      - path: nikoneko_run/Widgets
        excludes:
          - NikoNekoWidgetBundle.swift
          - Info.plist
    settings:
      base:
        INFOPLIST_FILE: nikoneko_run/App/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.fangyu.nikoneko
        SWIFT_VERSION: 6.0
        DEVELOPMENT_TEAM: B58R97M2WC
    entitlements:
      path: nikoneko_run/nikoneko.entitlements
      properties:
        com.apple.developer.icloud-services:
          - CloudKit
        com.apple.developer.icloud-container-identifiers:
          - iCloud.com.fangyu.nikoneko
        com.apple.security.application-groups:
          - group.com.fangyu.nikoneko-run
        com.apple.developer.healthkit: true
        com.apple.developer.healthkit.access: []
    info:
      path: nikoneko_run/App/Info.plist
      properties:
        CFBundleDisplayName: "Niconeko Run"
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSHealthShareUsageDescription: "Niconeko Run reads heart rate during runs."
        NSHealthUpdateUsageDescription: "Niconeko Run saves workout data to Health."
        NSMotionUsageDescription: "Niconeko Run tracks steps and distance."
        NSBluetoothAlwaysUsageDescription: "Niconeko Run connects to heart rate monitors."
        UIBackgroundModes:
          - audio
          - location
        NSLocationWhenInUseUsageDescription: "Niconeko Run uses location to track distance."
    capabilities:
      healthKit: {}
      backgroundModes:
        modes: [audio, location]
      push: {}
      appGroups:
        groups:
          - group.com.fangyu.nikoneko-run
      iCloud:
        services:
          - cloudKit
        containers:
          - iCloud.com.fangyu.nikoneko
    dependencies:
      - package: Lottie
        product: Lottie
    scheme:
      testTargets:
        - nikonekoTests

  nikonekoTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    bundleId: com.fangyu.nikoneko.tests
    sources:
      - path: nikoneko_run/Tests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fangyu.nikoneko.tests
        SWIFT_VERSION: 6.0
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: nikoneko

  NikoNekoWidgets:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    bundleId: com.fangyu.nikoneko.widgets
    sources:
      - path: nikoneko_run/Widgets
      - path: nikoneko_run/Core
        excludes:
          - Models/RunSession.swift
          - Models/UserProfile.swift
          - Models/ThresholdConfig.swift
          - WidgetTypes.swift
          - LanguageManager.swift
          - AppGroupDefaults+App.swift
      - path: nikoneko_run/Characters
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fangyu.nikoneko.widgets
        SWIFT_VERSION: 6.0
        NSExtensionPointIdentifier: com.apple.widgetkit-extension
    entitlements:
      path: nikoneko_run/NikoNekoWidgets.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.fangyu.nikoneko-run
    info:
      path: nikoneko_run/Widgets/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    dependencies:
      - target: nikoneko
        embed: false
